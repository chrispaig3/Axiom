; `exit` is Axiom's, not libc's: a raw exit_group/exit syscall.
(import IO)

(pub :: main Int)
;@axiom:effect(io)
(pub fn (main)
  {
    (printlnLit (__addr "exiting with 42"))
    (exit 42)
    0
  })
