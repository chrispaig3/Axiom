; The freestanding "hello world": no FFI, no libc, no printf.
(import IO)

(pub :: main Int)

;@axiom:effect(io)
(pub fn (main)
  {
    (printlnLit (__addr "hello, world"))
    (println "and hello again")
    0
  }
)
