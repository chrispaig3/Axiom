; The freestanding "hello world": no `foreign`, no libc, no printf.
(import IO)

(:: main Int)
;@axiom:effect(io)
(fn (main)
  {
    (printlnLit (__addr "hello, world"))
    (println (strFromLit (__addr "and hello again")))
    0
  })
