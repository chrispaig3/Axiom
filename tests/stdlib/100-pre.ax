(import Pre (when unless))

(import IO)

(pub :: main Int)

;@axiom:effect(io)
(pub fn (main)
  {
    (when (> 2 1) (printlnLit (__addr "when: pass")))
    (unless (> 1 2) (printlnLit (__addr "unless: pass")))
    0
  }
)
