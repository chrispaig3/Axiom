E AX3036 702-extern-type-discipline.ax:11:4-8 extern-type "an `extern` item cannot carry the type variable `a` across the boundary" ?"an `extern` signature names only `Int`, `Float`, `Bool`, `Char`, `String` and `Foreign` (one machine word each way); a Rust value of any other shape crosses as a `Foreign` handle, or through the wrapper `axiom-bindgen` generates (a `String` result, a `Result`, an `Option`, an opaque type)"
E AX3036 702-extern-type-discipline.ax:12:4-7 extern-type "an `extern` item cannot carry the type `Option` across the boundary" ?"an `extern` signature names only `Int`, `Float`, `Bool`, `Char`, `String` and `Foreign` (one machine word each way); a Rust value of any other shape crosses as a `Foreign` handle, or through the wrapper `axiom-bindgen` generates (a `String` result, a `Result`, an `Option`, an opaque type)"
E AX3036 702-extern-type-discipline.ax:13:4-7 extern-type "an `extern` item cannot carry a function-typed argument (a callback is an arrow over words only - `Int`, `Float`, `Bool`, `Char` - such as `(-> Int Float)`) across the boundary" ?"an `extern` signature names only `Int`, `Float`, `Bool`, `Char`, `String` and `Foreign` (one machine word each way); a Rust value of any other shape crosses as a `Foreign` handle, or through the wrapper `axiom-bindgen` generates (a `String` result, a `Result`, an `Option`, an opaque type)"
E AX3006 702-extern-type-discipline.ax:21:6-9 duplicate-definition "duplicate definition `add`" #"`add` redefined here" ^10:4-7:"`add` first defined here" ?"rename one of the definitions, or remove the duplicate"
