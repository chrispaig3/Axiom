E AX3005 557-match-unknown-scrutinee.ax:53:12-13 non-exhaustive-match "non-exhaustive pattern match: missing Tri" #"this `match` does not cover: Tri" ?"add the missing arms, or a wildcard `_` arm to catch the rest"
E AX3005 557-match-unknown-scrutinee.ax:60:10-11 non-exhaustive-match "this match can fall through: no arm is known to cover the scrutinee, and nothing catches the rest" #"no arm catches a non-matching value" !"a match no arm matched used to answer 0 silently at exit 0 - the result cell's zeroed contents, indistinguishable from a real answer (docs/memory-model.md MM-VAL-9b)" ?"add a `_` arm"
