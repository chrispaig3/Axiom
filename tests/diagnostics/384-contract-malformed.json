{"severity":"error","code":"AX3050","slug":"contract-malformed","message":"`(+ n 1)` is the `pre` on `wrongType` and has type `Int`, not `Bool`","file":"384-contract-malformed.ax","span":{"start":{"line":56,"col":13},"end":{"line":56,"col":20},"char_start":3198,"char_end":3205},"label":"","related":[],"notes":[],"help":["a `;@axiom:pre(...)` or `;@axiom:post(...)` is a claim the COMPILER cannot decide - there is no value analysis here - so it is compiled into the body and checked at run time, on every call, and a failure writes `axiom: precondition failed in `f`: ...` on fd 2 and exits 75. That is why the expression has to parse, has to be `Bool`, and has to PERFORM nothing: a contract that allocates or writes changes the program by being stated. Comparison, indexing, `strEq`, `strLen`, `vecLen` and `vecGet` are allocation-free and are what a predicate is written from; `strConcat`, `fmtInt` and `vecNew` are not. `result` names the declared result of a `post` and nothing anywhere else"],"expansion":[]}
{"severity":"error","code":"AX3050","slug":"contract-malformed","message":"`(> n 0) extra` is not one expression, and a contract is exactly one","file":"384-contract-malformed.ax","span":{"start":{"line":61,"col":13},"end":{"line":61,"col":26},"char_start":3270,"char_end":3283},"label":"","related":[],"notes":[],"help":["a `;@axiom:pre(...)` or `;@axiom:post(...)` is a claim the COMPILER cannot decide - there is no value analysis here - so it is compiled into the body and checked at run time, on every call, and a failure writes `axiom: precondition failed in `f`: ...` on fd 2 and exits 75. That is why the expression has to parse, has to be `Bool`, and has to PERFORM nothing: a contract that allocates or writes changes the program by being stated. Comparison, indexing, `strEq`, `strLen`, `vecLen` and `vecGet` are allocation-free and are what a predicate is written from; `strConcat`, `fmtInt` and `vecNew` are not. `result` names the declared result of a `post` and nothing anywhere else"],"expansion":[]}
{"severity":"error","code":"AX3050","slug":"contract-malformed","message":"`;@axiom:pre` states a contract with no expression","file":"384-contract-malformed.ax","span":{"start":{"line":66,"col":1},"end":{"line":66,"col":12},"char_start":3330,"char_end":3341},"label":"","related":[],"notes":[],"help":["a `;@axiom:pre(...)` or `;@axiom:post(...)` is a claim the COMPILER cannot decide - there is no value analysis here - so it is compiled into the body and checked at run time, on every call, and a failure writes `axiom: precondition failed in `f`: ...` on fd 2 and exits 75. That is why the expression has to parse, has to be `Bool`, and has to PERFORM nothing: a contract that allocates or writes changes the program by being stated. Comparison, indexing, `strEq`, `strLen`, `vecLen` and `vecGet` are allocation-free and are what a predicate is written from; `strConcat`, `fmtInt` and `vecNew` are not. `result` names the declared result of a `post` and nothing anywhere else"],"expansion":[]}
{"severity":"error","code":"AX3050","slug":"contract-malformed","message":"`result` names nothing in a `pre`: a precondition is checked BEFORE the body runs, so there is no result yet","file":"384-contract-malformed.ax","span":{"start":{"line":71,"col":16},"end":{"line":71,"col":22},"char_start":3404,"char_end":3410},"label":"","related":[],"notes":[],"help":["a `;@axiom:pre(...)` or `;@axiom:post(...)` is a claim the COMPILER cannot decide - there is no value analysis here - so it is compiled into the body and checked at run time, on every call, and a failure writes `axiom: precondition failed in `f`: ...` on fd 2 and exits 75. That is why the expression has to parse, has to be `Bool`, and has to PERFORM nothing: a contract that allocates or writes changes the program by being stated. Comparison, indexing, `strEq`, `strLen`, `vecLen` and `vecGet` are allocation-free and are what a predicate is written from; `strConcat`, `fmtInt` and `vecNew` are not. `result` names the declared result of a `post` and nothing anywhere else"],"expansion":[]}
{"severity":"error","code":"AX3050","slug":"contract-malformed","message":"`result` names nothing here: this declaration has no `::` signature, so it declares no result type for `result` to have","file":"384-contract-malformed.ax","span":{"start":{"line":76,"col":17},"end":{"line":76,"col":23},"char_start":3486,"char_end":3492},"label":"","related":[],"notes":[],"help":["a `;@axiom:pre(...)` or `;@axiom:post(...)` is a claim the COMPILER cannot decide - there is no value analysis here - so it is compiled into the body and checked at run time, on every call, and a failure writes `axiom: precondition failed in `f`: ...` on fd 2 and exits 75. That is why the expression has to parse, has to be `Bool`, and has to PERFORM nothing: a contract that allocates or writes changes the program by being stated. Comparison, indexing, `strEq`, `strLen`, `vecLen` and `vecGet` are allocation-free and are what a predicate is written from; `strConcat`, `fmtInt` and `vecNew` are not. `result` names the declared result of a `post` and nothing anywhere else"],"expansion":[]}
{"severity":"error","code":"AX3050","slug":"contract-malformed","message":"`(> (strLen (fmtInt n)) 0)` is the `pre` on `allocates` and performs Alloc, Mut","file":"384-contract-malformed.ax","span":{"start":{"line":79,"col":13},"end":{"line":79,"col":38},"char_start":3533,"char_end":3558},"label":"","related":[],"notes":[],"help":["a `;@axiom:pre(...)` or `;@axiom:post(...)` is a claim the COMPILER cannot decide - there is no value analysis here - so it is compiled into the body and checked at run time, on every call, and a failure writes `axiom: precondition failed in `f`: ...` on fd 2 and exits 75. That is why the expression has to parse, has to be `Bool`, and has to PERFORM nothing: a contract that allocates or writes changes the program by being stated. Comparison, indexing, `strEq`, `strLen`, `vecLen` and `vecGet` are allocation-free and are what a predicate is written from; `strConcat`, `fmtInt` and `vecNew` are not. `result` names the declared result of a `post` and nothing anywhere else"],"expansion":[]}
