E AX2001 627-if-missing-test.ax:5:15-16 unexpected-token "expected the test operand: `if` takes `(if test then else)`, variadically `(if t1 b1 t2 b2 ... els)`, found `)`" #"found `)` here" ?"Axiom expected the test operand: `if` takes `(if test then else)`, variadically `(if t1 b1 t2 b2 ... els)` at this position"
