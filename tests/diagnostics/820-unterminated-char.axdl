E AX1003 820-unterminated-char.ax:2:21-23 unterminated-char "unterminated char literal" #"character literal starts here but is never closed" ?"character literals must contain exactly one character, e.g. `'a'` or `'\\n'`"
