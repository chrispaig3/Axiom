E AX3027 DeclIn.ax:9:2-5 declaration-macro "a declaration-macro invocation inside an imported module is not supported yet" !"generated declarations in a module would need the module's mangling and visibility applied after import resolution has already run; entry-file invocation is the v1 surface, and this refusal is what keeps the limit loud" ?"invoke the macro from the entry file, or write the declarations directly"
