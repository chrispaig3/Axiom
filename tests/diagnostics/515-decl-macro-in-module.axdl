E AX3023 515-decl-macro-in-module.ax:38:9-15 private-name "module `DeclIn` declares `fromEntryMacro` but does not export it" #"not exported" !"an import's name list is checked against what the module exports" ?"write `pub` on its declaration"
E AX3023 515-decl-macro-in-module.ax:49:44-56 private-name "`hiddenInside` is private to module `DeclIn`" #"`hiddenInside` is not exported by its module" ?"a name leaves its module when its declaration is written `pub`; if the import that brought the module in carries a name list, the name has to appear there too"
E AX3023 515-decl-macro-in-module.ax:49:58-70 private-name "`alsoExported` is private to module `DeclIn`" #"`alsoExported` is not exported by its module" ?"a name leaves its module when its declaration is written `pub`; if the import that brought the module in carries a name list, the name has to appear there too"
E AX3023 515-decl-macro-in-module.ax:49:72-86 private-name "`fromEntryMacro` is private to module `DeclIn`" #"`fromEntryMacro` is not exported by its module" ?"a name leaves its module when its declaration is written `pub`; if the import that brought the module in carries a name list, the name has to appear there too"
