E AX3026 471-reserved-runtime-name.ax:9:6-17 reserved-runtime-name "`axiom_alloc` is the emitted runtime's allocator symbol; a program cannot define it" #"this name belongs to the runtime" !"the backend emits its own definition of `axiom_alloc` unconditionally; before this refusal the build died in `opt` as a duplicate symbol after `check` said OK" ?"rename the function - the override seam does not exist (docs/memory-model.md MM-ALLOC-8)"
