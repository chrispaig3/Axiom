E AX3018 605-macro-no-rule-matches.ax:39:2-6 macro-arity "no rule of macro `defN` matches this invocation; it accepts (defN (a b)) or (defN T 7)" !"rules are tried IN ORDER and the first whose pattern matches wins (macro-system.md MAC-LANG-18): an invocation matching none is this refusal rather than a fall-through. A plain identifier in a shape matches any argument; a literal matches its own VALUE, and a parenthesised shape matches a form of that many elements - but not a form headed by a keyword (`if`, `let`, `lambda`, `match`), which the parser gives a shape of its own" ?"write the invocation in one of the shapes above"
E AX3018 605-macro-no-rule-matches.ax:41:2-6 macro-arity "no rule of macro `defN` matches this invocation; it accepts (defN (a b)) or (defN T 7)" !"rules are tried IN ORDER and the first whose pattern matches wins (macro-system.md MAC-LANG-18): an invocation matching none is this refusal rather than a fall-through. A plain identifier in a shape matches any argument; a literal matches its own VALUE, and a parenthesised shape matches a form of that many elements - but not a form headed by a keyword (`if`, `let`, `lambda`, `match`), which the parser gives a shape of its own" ?"write the invocation in one of the shapes above"
E AX3018 605-macro-no-rule-matches.ax:43:2-6 macro-arity "no rule of macro `defN` matches this invocation; it accepts (defN (a b)) or (defN T 7)" !"rules are tried IN ORDER and the first whose pattern matches wins (macro-system.md MAC-LANG-18): an invocation matching none is this refusal rather than a fall-through. A plain identifier in a shape matches any argument; a literal matches its own VALUE, and a parenthesised shape matches a form of that many elements - but not a form headed by a keyword (`if`, `let`, `lambda`, `match`), which the parser gives a shape of its own" ?"write the invocation in one of the shapes above"
