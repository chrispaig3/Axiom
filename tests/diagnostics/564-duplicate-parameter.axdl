E AX3006 564-duplicate-parameter.ax:31:6-9 duplicate-definition "duplicate parameter `x`" #"`x` is already a parameter of this function" !"a reference resolves to the last binding, and codegen names an LLVM register after each parameter - two of a name emit the same register and the module is rejected" ?"give each parameter a distinct name; `_` binds a register too, so a second one needs its own spelling"
E AX3006 564-duplicate-parameter.ax:35:6-16 duplicate-definition "duplicate parameter `_`" #"`_` is already a parameter of this function" !"a reference resolves to the last binding, and codegen names an LLVM register after each parameter - two of a name emit the same register and the module is rejected" ?"give each parameter a distinct name; `_` binds a register too, so a second one needs its own spelling"
