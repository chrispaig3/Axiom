E AX2004 942-removed-consume.ax:1:12-19 removed-construct "`consume` parsed and reclaimed nothing, and is now refused" #"`consume` was refused rather than implemented: it was a parse-time IDENTITY that kept the `Linear` wrapper, so consuming twice was accepted and the form reclaimed nothing" ?"delete the wrapper and keep its argument - `(consume e)` always meant `e`, and a block is freed when its count reaches zero"
