E AX3025 415-trait-impl-shape.ax:10:4-8 trait-impl "trait `Eq` declares no method `nope`" #"in this implementation" !"an implementation may only define methods the trait declares" ?"check the spelling against the trait's `where` list"
E AX3025 415-trait-impl-shape.ax:6:8-10 trait-impl "this implementation of `Eq` does not define `ne`" #"in this implementation" !"a trait method with no implementation and no default has nothing to dispatch to" ?"define it, or give the trait method a default body with `= <expr>`"
E AX3025 415-trait-impl-shape.ax:15:8-15 trait-impl "no trait named `Missing` is in scope" #"in this implementation" !"an `impl` names the trait it implements; this one names nothing" ?"declare the trait, or import the module that declares it"
