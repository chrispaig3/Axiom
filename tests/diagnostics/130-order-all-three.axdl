E AX3006 130-order-all-three.ax:11:6-10 duplicate-definition "duplicate definition `main`" #"`main` redefined here" ^9:6-10:"`main` first defined here" ?"rename one of the definitions, or remove the duplicate"
E AX3015 130-order-all-three.ax:1:5-10 missing-definition "`ghost` has a signature but no definition" #"no `fn` defines `ghost`" ?"add the definition, or if it lives in another module, mark that definition `pub` so the import carries it"
E AX3013 130-order-all-three.ax:9:13-16 partial-application "partial application of `add`: it takes 2 argument(s) and 1 were supplied" #"`add` takes 2 argument(s); 1 more needed" !"a partial application has to hold the arguments it was not given, and a top-level function has no closure record to hold them in" ?"bind the missing arguments with a lambda: `(lambda (y) (f x y))` builds the value `(f x)` would mean"
