E AX3035 590-macro-binder-target.ax:24:7-12 macro-binder-target "a macro parameter used as a binder was given something that is not a variable" !"a `let`, `lambda` or pattern binder is a NAME, so a parameter standing in one can only be given a name - this is the same rule `set` targets follow (AX3022)" ?"pass a variable name for this parameter, or rewrite the template so the parameter is not a binder" &590-macro-binder-target.ax:12:9-14:"bind!"
E AX3035 590-macro-binder-target.ax:25:7-12 macro-binder-target "a macro parameter used as a binder was given something that is not a variable" !"a `let`, `lambda` or pattern binder is a NAME, so a parameter standing in one can only be given a name - this is the same rule `set` targets follow (AX3022)" ?"pass a variable name for this parameter, or rewrite the template so the parameter is not a binder" &590-macro-binder-target.ax:14:9-14:"pick!"
