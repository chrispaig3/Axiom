E AX3023 485-qualified-private-macro.ax:12:4-10 private-name "`VisAll::privMac` is private to module `VisAll`" #"`privMac` is not exported by its module" ?"a name leaves its module when its declaration is written `pub`; if the import that brought the module in carries a name list, the name has to appear there too"
