W AX3045 1005-recursion-in-scrutinee.ax:26:13-19 recursion-in-scrutinee "`total2` calls itself here, in the scrutinee of a `match`" #"recursive call to `total2`" ?"bind the scrutinee in a `let` and answer the recursive call from a match arm, where the self call is a tail call and the loop conversion fires"
W AX3045 1005-recursion-in-scrutinee.ax:38:14-18 recursion-in-scrutinee "`nest` calls itself here, in the scrutinee of a `match`" #"recursive call to `nest`" ?"bind the scrutinee in a `let` and answer the recursive call from a match arm, where the self call is a tail call and the loop conversion fires"
