E AX2001 633-for-pair-range.ax:8:27-28 unexpected-token "expected the container body: an element-index binder `(x i)` takes the container shape only `(for (x i) xs body)` - a range counts its own index, found `e`" #"found `e` here" ?"Axiom expected the container body: an element-index binder `(x i)` takes the container shape only `(for (x i) xs body)` - a range counts its own index at this position"
