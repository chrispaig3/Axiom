{"severity":"error","code":"AX3060","slug":"region-escape","message":"parameter `x`, which lives in region `@s`, is stored into the field `s` of a value, which lives in region `@r`","file":"645-region-escape-store.ax","span":{"start":{"line":48,"col":12},"end":{"line":48,"col":13},"char_start":2165,"char_end":2166},"label":"","related":[],"notes":[],"help":["MM-RGN-3 (docs/memory-model-v2-design.md): a value may be stored only into a place its own region OUTLIVES, because the place is still readable after the value's region has been reclaimed. A region named in a signature (`@r`) outlives the function's own region and is unordered against another name; the function's own region is its caller's. Keep the value in the region of the place it is stored into - allocate it there, or copy it there - or widen the place's annotation to the value's region"],"expansion":[]}
{"severity":"error","code":"AX3060","slug":"region-escape","message":"a value allocated in this function's own region is stored into parameter `v` of `vecPush`, which lives in region `@r` - `vecPush` stores a value it allocates into that parameter","file":"645-region-escape-store.ax","span":{"start":{"line":66,"col":6},"end":{"line":66,"col":13},"char_start":2355,"char_end":2362},"label":"","related":[],"notes":[],"help":["MM-RGN-3 (docs/memory-model-v2-design.md): a value may be stored only into a place its own region OUTLIVES, because the place is still readable after the value's region has been reclaimed. A region named in a signature (`@r`) outlives the function's own region and is unordered against another name; the function's own region is its caller's. Keep the value in the region of the place it is stored into - allocate it there, or copy it there - or widen the place's annotation to the value's region"],"expansion":[]}
{"severity":"error","code":"AX3060","slug":"region-escape","message":"parameter `x`, which lives in region `@s`, is stored into parameter `v` of `vecPush`, which lives in region `@r` - `vecPush` stores its parameter `x` into it","file":"645-region-escape-store.ax","span":{"start":{"line":66,"col":6},"end":{"line":66,"col":13},"char_start":2355,"char_end":2362},"label":"","related":[],"notes":[],"help":["MM-RGN-3 (docs/memory-model-v2-design.md): a value may be stored only into a place its own region OUTLIVES, because the place is still readable after the value's region has been reclaimed. A region named in a signature (`@r`) outlives the function's own region and is unordered against another name; the function's own region is its caller's. Keep the value in the region of the place it is stored into - allocate it there, or copy it there - or widen the place's annotation to the value's region"],"expansion":[]}
{"severity":"error","code":"AX3060","slug":"region-escape","message":"a value allocated in this function's own region is stored into parameter `v` of `vecPush`, which lives in region `@r` - `vecPush` stores a value it allocates into that parameter","file":"645-region-escape-store.ax","span":{"start":{"line":75,"col":6},"end":{"line":75,"col":13},"char_start":2456,"char_end":2463},"label":"","related":[],"notes":[],"help":["MM-RGN-3 (docs/memory-model-v2-design.md): a value may be stored only into a place its own region OUTLIVES, because the place is still readable after the value's region has been reclaimed. A region named in a signature (`@r`) outlives the function's own region and is unordered against another name; the function's own region is its caller's. Keep the value in the region of the place it is stored into - allocate it there, or copy it there - or widen the place's annotation to the value's region"],"expansion":[]}
{"severity":"error","code":"AX3060","slug":"region-escape","message":"a value allocated in this function's own region is stored into parameter `v` of `vecPush`, which lives in region `@r` - `vecPush` stores its parameter `x` into it","file":"645-region-escape-store.ax","span":{"start":{"line":75,"col":6},"end":{"line":75,"col":13},"char_start":2456,"char_end":2463},"label":"","related":[],"notes":[],"help":["MM-RGN-3 (docs/memory-model-v2-design.md): a value may be stored only into a place its own region OUTLIVES, because the place is still readable after the value's region has been reclaimed. A region named in a signature (`@r`) outlives the function's own region and is unordered against another name; the function's own region is its caller's. Keep the value in the region of the place it is stored into - allocate it there, or copy it there - or widen the place's annotation to the value's region"],"expansion":[]}
