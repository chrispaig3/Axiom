{"severity":"error","code":"AX3050","slug":"contract-malformed","message":"`(+ n 1)` is the `pre` on `wrongType` and has type `Int`, not `Bool`","file":"385-contract-malformed.ax","span":{"start":{"line":70,"col":13},"end":{"line":70,"col":20},"char_start":4149,"char_end":4156},"label":"","related":[],"notes":[],"help":["a `;@axiom:pre(...)` or `;@axiom:post(...)` is a claim the COMPILER cannot decide - there is no value analysis here - so it is compiled into the body and checked at run time, on every call, and a failure writes `axiom: precondition failed in `f`: ...` on fd 2 and exits 77. That is why the expression has to parse, has to be `Bool`, and has to PERFORM nothing: a contract that allocates or writes changes the program by being stated. Comparison, indexing, `strEq`, `strLen`, `vecLen` and `vecGet` are allocation-free and are what a predicate is written from; `strConcat`, `fmtInt` and `vecNew` are not. `result` names the declared result of a `post` and nothing anywhere else"],"expansion":[]}
{"severity":"error","code":"AX3050","slug":"contract-malformed","message":"`(> n 0) extra` is not one expression, and a contract is exactly one","file":"385-contract-malformed.ax","span":{"start":{"line":75,"col":13},"end":{"line":75,"col":26},"char_start":4221,"char_end":4234},"label":"","related":[],"notes":[],"help":["a `;@axiom:pre(...)` or `;@axiom:post(...)` is a claim the COMPILER cannot decide - there is no value analysis here - so it is compiled into the body and checked at run time, on every call, and a failure writes `axiom: precondition failed in `f`: ...` on fd 2 and exits 77. That is why the expression has to parse, has to be `Bool`, and has to PERFORM nothing: a contract that allocates or writes changes the program by being stated. Comparison, indexing, `strEq`, `strLen`, `vecLen` and `vecGet` are allocation-free and are what a predicate is written from; `strConcat`, `fmtInt` and `vecNew` are not. `result` names the declared result of a `post` and nothing anywhere else"],"expansion":[]}
{"severity":"error","code":"AX3050","slug":"contract-malformed","message":"`;@axiom:pre` states a contract with no expression","file":"385-contract-malformed.ax","span":{"start":{"line":80,"col":1},"end":{"line":80,"col":12},"char_start":4281,"char_end":4292},"label":"","related":[],"notes":[],"help":["a `;@axiom:pre(...)` or `;@axiom:post(...)` is a claim the COMPILER cannot decide - there is no value analysis here - so it is compiled into the body and checked at run time, on every call, and a failure writes `axiom: precondition failed in `f`: ...` on fd 2 and exits 77. That is why the expression has to parse, has to be `Bool`, and has to PERFORM nothing: a contract that allocates or writes changes the program by being stated. Comparison, indexing, `strEq`, `strLen`, `vecLen` and `vecGet` are allocation-free and are what a predicate is written from; `strConcat`, `fmtInt` and `vecNew` are not. `result` names the declared result of a `post` and nothing anywhere else"],"expansion":[]}
{"severity":"error","code":"AX3050","slug":"contract-malformed","message":"`result` names nothing in a `pre`: a precondition is checked BEFORE the body runs, so there is no result yet","file":"385-contract-malformed.ax","span":{"start":{"line":85,"col":16},"end":{"line":85,"col":22},"char_start":4355,"char_end":4361},"label":"","related":[],"notes":[],"help":["a `;@axiom:pre(...)` or `;@axiom:post(...)` is a claim the COMPILER cannot decide - there is no value analysis here - so it is compiled into the body and checked at run time, on every call, and a failure writes `axiom: precondition failed in `f`: ...` on fd 2 and exits 77. That is why the expression has to parse, has to be `Bool`, and has to PERFORM nothing: a contract that allocates or writes changes the program by being stated. Comparison, indexing, `strEq`, `strLen`, `vecLen` and `vecGet` are allocation-free and are what a predicate is written from; `strConcat`, `fmtInt` and `vecNew` are not. `result` names the declared result of a `post` and nothing anywhere else"],"expansion":[]}
{"severity":"error","code":"AX3050","slug":"contract-malformed","message":"`result` names nothing here: this declaration has no `::` signature, so it declares no result type for `result` to have","file":"385-contract-malformed.ax","span":{"start":{"line":90,"col":17},"end":{"line":90,"col":23},"char_start":4437,"char_end":4443},"label":"","related":[],"notes":[],"help":["a `;@axiom:pre(...)` or `;@axiom:post(...)` is a claim the COMPILER cannot decide - there is no value analysis here - so it is compiled into the body and checked at run time, on every call, and a failure writes `axiom: precondition failed in `f`: ...` on fd 2 and exits 77. That is why the expression has to parse, has to be `Bool`, and has to PERFORM nothing: a contract that allocates or writes changes the program by being stated. Comparison, indexing, `strEq`, `strLen`, `vecLen` and `vecGet` are allocation-free and are what a predicate is written from; `strConcat`, `fmtInt` and `vecNew` are not. `result` names the declared result of a `post` and nothing anywhere else"],"expansion":[]}
{"severity":"error","code":"AX3050","slug":"contract-malformed","message":"`(> (strLen (fmtInt n)) 0)` is the `pre` on `allocates` and performs Alloc, Mut","file":"385-contract-malformed.ax","span":{"start":{"line":93,"col":13},"end":{"line":93,"col":38},"char_start":4484,"char_end":4509},"label":"","related":[],"notes":[],"help":["a `;@axiom:pre(...)` or `;@axiom:post(...)` is a claim the COMPILER cannot decide - there is no value analysis here - so it is compiled into the body and checked at run time, on every call, and a failure writes `axiom: precondition failed in `f`: ...` on fd 2 and exits 77. That is why the expression has to parse, has to be `Bool`, and has to PERFORM nothing: a contract that allocates or writes changes the program by being stated. Comparison, indexing, `strEq`, `strLen`, `vecLen` and `vecGet` are allocation-free and are what a predicate is written from; `strConcat`, `fmtInt` and `vecNew` are not. `result` names the declared result of a `post` and nothing anywhere else"],"expansion":[]}
{"severity":"error","code":"AX3050","slug":"contract-malformed","message":"`(> (boxed n) 0)` is the `pre` on `constructs` and performs Alloc","file":"385-contract-malformed.ax","span":{"start":{"line":133,"col":13},"end":{"line":133,"col":28},"char_start":5119,"char_end":5134},"label":"","related":[],"notes":[],"help":["a `;@axiom:pre(...)` or `;@axiom:post(...)` is a claim the COMPILER cannot decide - there is no value analysis here - so it is compiled into the body and checked at run time, on every call, and a failure writes `axiom: precondition failed in `f`: ...` on fd 2 and exits 77. That is why the expression has to parse, has to be `Bool`, and has to PERFORM nothing: a contract that allocates or writes changes the program by being stated. Comparison, indexing, `strEq`, `strLen`, `vecLen` and `vecGet` are allocation-free and are what a predicate is written from; `strConcat`, `fmtInt` and `vecNew` are not. `result` names the declared result of a `post` and nothing anywhere else"],"expansion":[]}
