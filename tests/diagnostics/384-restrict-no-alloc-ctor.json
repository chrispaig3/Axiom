{"severity":"error","code":"AX3049","slug":"restriction-violated","message":"`mk` claims `restrict(no-alloc)` and the body performs Alloc in its own body (a constructor application, an `alloc` form, or a `handle` installing a handler)","file":"384-restrict-no-alloc-ctor.ax","span":{"start":{"line":74,"col":6},"end":{"line":74,"col":8},"char_start":3769,"char_end":3771},"related":[],"notes":[],"help":["a restriction is a CLAIM the author wrote, and this is the compiler answering it. `no-io`, `no-alloc`, `no-unsafe`, `no-foreign`, `no-cast:deep` and `no-recursion` are transitive - the effect row and the call graph are fixpoints over every callee - and the path in the message is the chain of resolved calls (`symbols --calls` spells each hop the same way) from this declaration to where the effect enters, or around the cycle, so the fix is at a named place rather than somewhere below. Make the body keep the claim, or delete the tag, which WITHDRAWS the claim rather than answering it; an unrestricted function is never asked"],"expansion":[]}
{"severity":"error","code":"AX3049","slug":"restriction-violated","message":"`pair` claims `restrict(no-alloc)` and the body performs Alloc in its own body (a constructor application, an `alloc` form, or a `handle` installing a handler)","file":"384-restrict-no-alloc-ctor.ax","span":{"start":{"line":81,"col":6},"end":{"line":81,"col":10},"char_start":3882,"char_end":3886},"related":[],"notes":[],"help":["a restriction is a CLAIM the author wrote, and this is the compiler answering it. `no-io`, `no-alloc`, `no-unsafe`, `no-foreign`, `no-cast:deep` and `no-recursion` are transitive - the effect row and the call graph are fixpoints over every callee - and the path in the message is the chain of resolved calls (`symbols --calls` spells each hop the same way) from this declaration to where the effect enters, or around the cycle, so the fix is at a named place rather than somewhere below. Make the body keep the claim, or delete the tag, which WITHDRAWS the claim rather than answering it; an unrestricted function is never asked"],"expansion":[]}
{"severity":"error","code":"AX3049","slug":"restriction-violated","message":"`kw` claims `restrict(no-alloc)` and the body performs Alloc in its own body (a constructor application, an `alloc` form, or a `handle` installing a handler)","file":"384-restrict-no-alloc-ctor.ax","span":{"start":{"line":86,"col":6},"end":{"line":86,"col":8},"char_start":3953,"char_end":3955},"related":[],"notes":[],"help":["a restriction is a CLAIM the author wrote, and this is the compiler answering it. `no-io`, `no-alloc`, `no-unsafe`, `no-foreign`, `no-cast:deep` and `no-recursion` are transitive - the effect row and the call graph are fixpoints over every callee - and the path in the message is the chain of resolved calls (`symbols --calls` spells each hop the same way) from this declaration to where the effect enters, or around the cycle, so the fix is at a named place rather than somewhere below. Make the body keep the claim, or delete the tag, which WITHDRAWS the claim rather than answering it; an unrestricted function is never asked"],"expansion":[]}
{"severity":"error","code":"AX3049","slug":"restriction-violated","message":"`held` claims `restrict(no-alloc)` and the body performs Alloc in its own body (a constructor application, an `alloc` form, or a `handle` installing a handler)","file":"384-restrict-no-alloc-ctor.ax","span":{"start":{"line":106,"col":6},"end":{"line":106,"col":10},"char_start":4270,"char_end":4274},"related":[],"notes":[],"help":["a restriction is a CLAIM the author wrote, and this is the compiler answering it. `no-io`, `no-alloc`, `no-unsafe`, `no-foreign`, `no-cast:deep` and `no-recursion` are transitive - the effect row and the call graph are fixpoints over every callee - and the path in the message is the chain of resolved calls (`symbols --calls` spells each hop the same way) from this declaration to where the effect enters, or around the cycle, so the fix is at a named place rather than somewhere below. Make the body keep the claim, or delete the tag, which WITHDRAWS the claim rather than answering it; an unrestricted function is never asked"],"expansion":[]}
