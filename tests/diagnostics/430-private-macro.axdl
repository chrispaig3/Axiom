E AX3023 430-private-macro.ax:5:27-34 private-name "`privMac` is private to module `VisAll`" #"`privMac` is not exported by its module" ?"a name leaves its module when its declaration is written `pub`; if the import that brought the module in carries a name list, the name has to appear there too"
