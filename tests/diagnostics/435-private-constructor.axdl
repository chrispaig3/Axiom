E AX3023 435-private-constructor.ax:6:11-15 private-name "`Wrap` is private to module `VisAll`" #"`Wrap` is not exported by its module" ?"a name leaves its module when its declaration is written `pub`; if the import that brought the module in carries a name list, the name has to appear there too"
E AX3023 435-private-constructor.ax:7:7-11 private-name "`Wrap` is private to module `VisAll`" #"`Wrap` is not exported by its module" ?"a name leaves its module when its declaration is written `pub`; if the import that brought the module in carries a name list, the name has to appear there too"
