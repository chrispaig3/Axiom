E AX3026 472-reserved-retain.ax:7:6-18 reserved-runtime-name "`axiom_retain` is the emitted runtime's ownership primitive; a program cannot define it" #"this name belongs to the runtime" !"the backend emits its own definition unconditionally, exactly as it does `axiom_alloc`'s" ?"rename the function - retain and release belong to the reference-counting runtime (docs/memory-model.md MM-LIFE-2b)"
E AX3026 472-reserved-retain.ax:9:6-19 reserved-runtime-name "`axiom_release` is the emitted runtime's ownership primitive; a program cannot define it" #"this name belongs to the runtime" !"the backend emits its own definition unconditionally, exactly as it does `axiom_alloc`'s" ?"rename the function - retain and release belong to the reference-counting runtime (docs/memory-model.md MM-LIFE-2b)"
