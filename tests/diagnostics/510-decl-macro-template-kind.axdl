E AX3021 510-decl-macro-template-kind.ax:8:10-12 macro-template-unsupported "macro `defData`'s template may generate `fn` and `::` declarations, further macro invocations, or a syntax/for over them - this form is not supported yet" !"data, struct, trait, impl, effect and import templates refuse here, at the macro's own line, rather than passing through silently (MAC-CAP-2)" ?"generate the declaration by hand, or split it out of the template"
