; `[T]` checked in signatures while no literal, no pattern and no
; runtime representation existed behind it. Sequences are `(Vec T)`,
; so the brackets are refused with migration advice rather than
; parsed into a shape no value can inhabit.
;
; `.axbad` because the refusal is at parse time.
(:: takes (-> [Int] Int))
