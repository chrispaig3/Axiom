E AX1001 930-tab-indent.ax:3:7-8 unexpected-char "unexpected character `$`" #"`$` is not valid here" ?"an identifier is letters, digits, `_`, `'` and `+ - * / % < > = ! & | ^`, and cannot begin with a digit or `'`; operators are not a separate class - `+` is simply an identifier - and `?`, `~` and `@` are not identifier characters at all"
