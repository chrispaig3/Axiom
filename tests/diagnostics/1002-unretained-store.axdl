E AX3071 1002-unretained-store.ax:10:10-14 unretained-store "storing a `String` with a bare `__store64` takes no share: the owner's release frees a block this word still names" #"storing `String`" !"a raw `__store64` writes the word and nothing else; the release walk never counted a share it cannot see" ?"store through `memSetWord`, which retains first - or `Ffi.ax`'s handle paths for handles"
