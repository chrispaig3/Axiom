E AX3023 370-private-name.ax:5:26-32 private-name "`helper` is private to module `PrivHelp`" #"`helper` is not exported by its module" ?"a name leaves its module when its declaration is written `pub`; if the import that brought the module in carries a name list, the name has to appear there too"
