E AX3029 481-record-bitmap-capacity.ax:8:9-13 record-bitmap-capacity "struct `Wide` has 48 fields, and a block's reference map covers 47" #"too wide for one reference map" !"the shape word packs one bit per payload word - is this word a reference the release path must follow - beside the form bit and the word count; bits 16..62 give 47 mappable words, and the widest declaration in this repository is the compiler's own 36-field CG record (docs/memory-model.md MM-LIFE-2d)" ?"split the declaration: a nested struct or constructor cell is ONE word of the outer block, and every block carries its own map"
E AX3029 481-record-bitmap-capacity.ax:59:4-6 record-bitmap-capacity "constructor `WD` has 47 fields, and a cell's reference map covers 46 beside the tag word" #"too wide for one reference map" !"the shape word packs one bit per payload word - is this word a reference the release path must follow - beside the form bit and the word count; bits 16..62 give 47 mappable words, the tag takes the first, and the widest declaration in this repository is the compiler's own 36-field CG record (docs/memory-model.md MM-LIFE-2d)" ?"split the declaration: a nested struct or constructor cell is ONE word of the outer block, and every block carries its own map"
