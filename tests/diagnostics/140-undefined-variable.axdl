E AX3001 140-undefined-variable.ax:3:15-19 undefined-variable "undefined variable `zzz1`" ?"variables must be defined (via `define`/`fn`, a `let` binding, or a lambda parameter) before they are used; check for typos"
E AX3001 140-undefined-variable.ax:5:16-20 undefined-variable "undefined variable `zzz2`" ?"variables must be defined (via `define`/`fn`, a `let` binding, or a lambda parameter) before they are used; check for typos"
E AX3001 140-undefined-variable.ax:5:21-25 undefined-variable "undefined variable `zzz3`" ?"variables must be defined (via `define`/`fn`, a `let` binding, or a lambda parameter) before they are used; check for typos"
E AX3001 140-undefined-variable.ax:5:32-36 undefined-variable "undefined variable `zzz2`" ?"variables must be defined (via `define`/`fn`, a `let` binding, or a lambda parameter) before they are used; check for typos"
