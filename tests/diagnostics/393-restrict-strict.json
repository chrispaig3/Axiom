{"severity":"warning","code":"AX3051","slug":"restriction-unverifiable","message":"`softIo` claims `restrict(no-io)` and the claim cannot be checked: the body CALLS its own parameter `f`, so what it performs is decided by the argument each caller passes - the effect row is complete about this body and says nothing about that call","file":"393-restrict-strict.ax","span":{"start":{"line":74,"col":6},"end":{"line":74,"col":12},"char_start":3500,"char_end":3506},"label":"","related":[],"notes":[],"help":["a limit of the ANALYSIS rather than a defect in the program - AX3037's argument at one remove. There are four readings: an effect present only as POSSIBLE, a row the walk marked incomplete, a body that CALLS one of its own parameters (`#effect-params=`), whose answer is chosen by each caller and cannot be settled here at all, and - for `no-escape` alone - a region fixpoint that hit its round limit before the facts settled, which is a depth limit rather than anything about this declaration. Name the function at the call, stop calling the parameter, shorten the chain, or drop the claim; `no-cast` and `no-wrap` are checked regardless, because both are lexical and need no call resolved"],"expansion":[]}
{"severity":"error","code":"AX3057","slug":"restriction-unproven","message":"`hardIo` claims `restrict(no-io, strict)` and the claim cannot be PROVEN: the body CALLS its own parameter `f`, so what it performs is decided by the argument each caller passes - the effect row is complete about this body and says nothing about that call","file":"393-restrict-strict.ax","span":{"start":{"line":59,"col":6},"end":{"line":59,"col":12},"char_start":3202,"char_end":3208},"label":"","related":[],"notes":[],"help":["`strict` asks for a guarantee and not a best effort: where the walk cannot settle the claim, it is refused rather than warned. The four readings are AX3051's - an effect present only as POSSIBLE, a row the walk marked incomplete, a body that CALLS one of its own parameters (`#effect-params=`), whose answer is chosen by each caller, and a region fixpoint that hit its round limit. Name the function at the call site, stop calling the parameter, shorten the chain, or drop `strict` to keep the claim as a checked best effort (AX3051)"],"expansion":[]}
{"severity":"error","code":"AX3057","slug":"restriction-unproven","message":"`hardAlloc` claims `restrict(no-alloc, strict)` and the claim cannot be PROVEN: the body CALLS its own parameter `f`, so what it performs is decided by the argument each caller passes - the effect row is complete about this body and says nothing about that call","file":"393-restrict-strict.ax","span":{"start":{"line":64,"col":6},"end":{"line":64,"col":15},"char_start":3304,"char_end":3313},"label":"","related":[],"notes":[],"help":["`strict` asks for a guarantee and not a best effort: where the walk cannot settle the claim, it is refused rather than warned. The four readings are AX3051's - an effect present only as POSSIBLE, a row the walk marked incomplete, a body that CALLS one of its own parameters (`#effect-params=`), whose answer is chosen by each caller, and a region fixpoint that hit its round limit. Name the function at the call site, stop calling the parameter, shorten the chain, or drop `strict` to keep the claim as a checked best effort (AX3051)"],"expansion":[]}
{"severity":"error","code":"AX3057","slug":"restriction-unproven","message":"`hardRec` claims `restrict(no-recursion, strict)` and the claim cannot be PROVEN: the body CALLS its own parameter `f`, so what it performs is decided by the argument each caller passes - the effect row is complete about this body and says nothing about that call","file":"393-restrict-strict.ax","span":{"start":{"line":69,"col":6},"end":{"line":69,"col":13},"char_start":3411,"char_end":3418},"label":"","related":[],"notes":[],"help":["`strict` asks for a guarantee and not a best effort: where the walk cannot settle the claim, it is refused rather than warned. The four readings are AX3051's - an effect present only as POSSIBLE, a row the walk marked incomplete, a body that CALLS one of its own parameters (`#effect-params=`), whose answer is chosen by each caller, and a region fixpoint that hit its round limit. Name the function at the call site, stop calling the parameter, shorten the chain, or drop `strict` to keep the claim as a checked best effort (AX3051)"],"expansion":[]}
{"severity":"error","code":"AX3049","slug":"restriction-violated","message":"`refuted` claims `restrict(no-io)` and the body performs IO: refuted -> IO$writeStr -> Sys$sysWriteAllFd -> Sys$sysWriteFd -> __syscall3","file":"393-restrict-strict.ax","span":{"start":{"line":86,"col":6},"end":{"line":86,"col":13},"char_start":3709,"char_end":3716},"label":"","related":[],"notes":[],"help":["a restriction is a CLAIM the author wrote, and this is the compiler answering it. `no-io`, `no-alloc`, `no-foreign`, `no-cast:deep` and `no-recursion` are transitive - the effect row and the call graph are fixpoints over every callee - and the path in the message is the chain of resolved calls (`symbols --calls` spells each hop the same way) from this declaration to where the effect enters, or around the cycle, so the fix is at a named place rather than somewhere below. Make the body keep the claim, or delete the tag, which WITHDRAWS the claim rather than answering it; an unrestricted function is never asked"],"expansion":[]}
