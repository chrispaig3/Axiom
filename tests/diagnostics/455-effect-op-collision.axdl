W AX3053 455-effect-op-collision.ax:19:6-10 unhandled-operation "`main` reaches operation `two` of effect `Two` with no handler: the process would write `axiom: unhandled effect` and exit 71 there - in `main`'s own body" #"reaches `Two` with nothing handling it" ?"install one - `(handle BODY (E) HANDLER)` around the call - or, if an unhandled operation of this effect is a deliberate abort as `Assert`'s is, write `;@axiom:unhandled(trap)` above its `effect` declaration"
E AX3006 455-effect-op-collision.ax:15:6-9 duplicate-definition "duplicate definition `two`" #"`two` redefined here" ^11:4-7:"`two` first defined here" ?"rename one of the definitions, or remove the duplicate"
