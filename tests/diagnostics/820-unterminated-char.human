error[AX1003]: unterminated char literal
 --> 820-unterminated-char.ax:2:21
  |
2 | (fn (main) (let ((c 'a)) 0))
  |                     ^^ character literal starts here but is never closed
  |
  = help: character literals must contain exactly one character, e.g. `'a'` or `'\n'`
  = help: run `axiom explain AX1003` for a full explanation

compilation failed due to 1 previous error
