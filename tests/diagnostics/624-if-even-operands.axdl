E AX2001 624-if-even-operands.ax:5:39-40 unexpected-token "expected the else operand: `if` takes test-branch pairs and a final else `(if t1 b1 t2 b2 ... els)` - write it out, or continue with the next test, found `)`" #"found `)` here" ?"Axiom expected the else operand: `if` takes test-branch pairs and a final else `(if t1 b1 t2 b2 ... els)` - write it out, or continue with the next test at this position"
