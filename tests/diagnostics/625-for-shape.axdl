E AX2001 625-for-shape.ax:19:27-28 unexpected-token "expected `)` - `for` takes `(for x xs body)`, `(for (x i) xs body)`, `(for i lo hi body)` or `(for i lo hi step body)`, and `{ ... }` sequences several, found `3`" #"found `3` here" ?"Axiom expected `)` - `for` takes `(for x xs body)`, `(for (x i) xs body)`, `(for i lo hi body)` or `(for i lo hi step body)`, and `{ ... }` sequences several at this position"
