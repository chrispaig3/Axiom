[1;31merror[AX3064][0m[1m: a concurrent binding captures `s`, which has type `String` - a reference the parent also holds[0m
  [1;34m--> [0m644-parallel-thunk-shape.ax:44:34
[1;34m   |[0m
[1;34m44 |[0m         (lambda (w) (+ w (strLen s)))
[1;34m   |[0m                                  [1;31m^ `s` is bound outside this binding[0m
[1;34m   |[0m
  [1;34m = [0m[1mnote: [0mMM-PAR-6: a binding runs BESIDE its parent, and `axiom_retain`/`axiom_release` are a plain load-add-store rather than an `atomicrmw` - two threads touching one block's count lose an increment and free a block a live reference still names. The rule is the language's and not the lowering's, so it does not depend on `--threads`; `__proc_spawn` names the isolated lowering and is exempt
  [1;34m = [0m[1;32mhelp: [0mpass the value in through the thunk's word argument instead of capturing it, or build a copy of it inside the binding: what the binding may share with its parent is a word
  [1;34m = [0m[1;32mhelp: [0mrun `axiom explain AX3064` for a full explanation

[1;31merror[AX3064][0m[1m: a concurrent binding spawns a call result, whose captures this checker cannot see[0m
  [1;34m--> [0m644-parallel-thunk-shape.ax:51:29
[1;34m   |[0m
[1;34m51 |[0m   (__par_join (__par_spawn (mk s) 0)))
[1;34m   |[0m                             [1;31m^^ the thunk's captures are not visible here[0m
[1;34m   |[0m
  [1;34m = [0m[1mnote: [0mMM-PAR-6: a binding runs BESIDE its parent, and `axiom_retain`/`axiom_release` are a plain load-add-store rather than an `atomicrmw` - two threads touching one block's count lose an increment and free a block a live reference still names. The rule is the language's and not the lowering's, so it does not depend on `--threads`; `__proc_spawn` names the isolated lowering and is exempt
  [1;34m = [0m[1;32mhelp: [0mwrite the lambda at the spawn, or name a top-level function: a literal is checked where it stands and a top-level name captures nothing
  [1;34m = [0m[1;32mhelp: [0mrun `axiom explain AX3064` for a full explanation

[1;31merror[AX3064][0m[1m: a concurrent binding spawns a field, whose captures this checker cannot see[0m
  [1;34m--> [0m644-parallel-thunk-shape.ax:59:32
[1;34m   |[0m
[1;34m59 |[0m     (__par_join (__par_spawn b.f 0))))
[1;34m   |[0m                                [1;31m^ the thunk's captures are not visible here[0m
[1;34m   |[0m
  [1;34m = [0m[1mnote: [0mMM-PAR-6: a binding runs BESIDE its parent, and `axiom_retain`/`axiom_release` are a plain load-add-store rather than an `atomicrmw` - two threads touching one block's count lose an increment and free a block a live reference still names. The rule is the language's and not the lowering's, so it does not depend on `--threads`; `__proc_spawn` names the isolated lowering and is exempt
  [1;34m = [0m[1;32mhelp: [0mwrite the lambda at the spawn, or name a top-level function: a literal is checked where it stands and a top-level name captures nothing
  [1;34m = [0m[1;32mhelp: [0mrun `axiom explain AX3064` for a full explanation

[1;31merror[AX3064][0m[1m: a concurrent binding captures `s`, which has type `String` - a reference the parent also holds[0m
  [1;34m--> [0m644-parallel-thunk-shape.ax:66:24
[1;34m   |[0m
[1;34m66 |[0m       (let ((k (strLen s)))
[1;34m   |[0m                        [1;31m^ `s` is bound outside this binding[0m
[1;34m   |[0m
  [1;34m = [0m[1mnote: [0mMM-PAR-6: a binding runs BESIDE its parent, and `axiom_retain`/`axiom_release` are a plain load-add-store rather than an `atomicrmw` - two threads touching one block's count lose an increment and free a block a live reference still names. The rule is the language's and not the lowering's, so it does not depend on `--threads`; `__proc_spawn` names the isolated lowering and is exempt
  [1;34m = [0m[1;32mhelp: [0mpass the value in through the thunk's word argument instead of capturing it, or build a copy of it inside the binding: what the binding may share with its parent is a word
  [1;34m = [0m[1;32mhelp: [0mrun `axiom explain AX3064` for a full explanation

compilation failed due to 4 previous errors
