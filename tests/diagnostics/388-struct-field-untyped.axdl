E AX3056 388-struct-field-untyped.ax:28:4-7 struct-field-untyped "field `msg` of struct `NoColon` declares no type; a field's type is what decides whether the release path follows it" #"no `:` type after the field's name" ?"write `(msg : Type)` - the `:` is what makes this a field declaration"
E AX3056 388-struct-field-untyped.ax:32:4-7 struct-field-untyped "field `odd` of struct `NotAType` declares no type; a field's type is what decides whether the release path follows it" #"no `:` type after the field's name" ?"write `(odd : Type)` - the `:` is what makes this a field declaration"
E AX3056 388-struct-field-untyped.ax:36:4-7 struct-field-untyped "field `msg` of struct `BareName` declares no type; a field's type is what decides whether the release path follows it" #"no `:` type after the field's name" ?"write `(msg : Type)` - the `:` is what makes this a field declaration"
