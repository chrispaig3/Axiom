E AX3028 560-syntax-scalar-misuse.ax:39:2-10 syntax-query "no constructor named `Ghost` for syntax/arity to measure" !"the query vocabulary is CLOSED and compiler-implemented (macro-system.md MAC-CAP-5/6): every query is answered from the declaration list at expansion time, and a query with no answer is this refusal, never a default value" ?"the query reads the declaration list; syntax/arity's first argument names a data constructor"
E AX3028 560-syntax-scalar-misuse.ax:41:2-9 syntax-query "`syntax/name` is not a query this position answers" !"the query vocabulary is CLOSED and compiler-implemented (macro-system.md MAC-CAP-5/6): every query is answered from the declaration list at expansion time, and a query with no answer is this refusal, never a default value" ?"every query is APPLIED - `(syntax/name x)`, `(syntax/constructors T)` - so a bare `syntax/` name is a query missing its arguments, or a spelling the vocabulary does not answer"
E AX3028 560-syntax-scalar-misuse.ax:34:33-34 syntax-query "a syntax/* query's argument must be a bare identifier or a `(syntax/join a b)`" !"the query vocabulary is CLOSED and compiler-implemented (macro-system.md MAC-CAP-5/6): every query is answered from the declaration list at expansion time, and a query with no answer is this refusal, never a default value" ?"pass a plain name here, or join two of them with `(syntax/join a b)`"
E AX3028 560-syntax-scalar-misuse.ax:45:2-9 syntax-query "syntax/join takes exactly two identifiers" !"the query vocabulary is CLOSED and compiler-implemented (macro-system.md MAC-CAP-5/6): every query is answered from the declaration list at expansion time, and a query with no answer is this refusal, never a default value" ?"write `(syntax/join a b)`, or `((syntax/join a b) x)` to call what it names"
