W AX3039 346-axtag-key-typo.ax:20:6-13 axtag-key-typo "unrecognised AXTAG key `pur` on `slipped`; did you mean `pure`?" ?"an unrecognised key is recorded and never checked, so the claim this looks like was not made; the keys that are checked are `pure` and `effect(...)`"
W AX3039 346-axtag-key-typo.ax:25:6-13 axtag-key-typo "unrecognised AXTAG key `PURE` on `shouted`; did you mean `pure`?" ?"an unrecognised key is recorded and never checked, so the claim this looks like was not made; the keys that are checked are `pure` and `effect(...)`"
W AX3039 346-axtag-key-typo.ax:30:6-11 axtag-key-typo "unrecognised AXTAG key `effct` on `other`; did you mean `effect`?" ?"an unrecognised key is recorded and never checked, so the claim this looks like was not made; the keys that are checked are `pure` and `effect(...)`"
