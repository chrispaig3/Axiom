W AX3039 346-axtag-key-typo.ax:21:6-13 axtag-key-typo "AXTAG key typo on `slipped`: unrecognised key `pur`; did you mean `pure`?" ?"an unrecognised key is recorded and never checked, so the claim this looks like was not made; the keys that are checked are `pure`, `effect(...)`, `restrict(...)`, `pre(...)`, `post(...)` and `unhandled(trap)` on an `effect` declaration"
W AX3039 346-axtag-key-typo.ax:26:6-13 axtag-key-typo "AXTAG key typo on `shouted`: unrecognised key `PURE`; did you mean `pure`?" ?"an unrecognised key is recorded and never checked, so the claim this looks like was not made; the keys that are checked are `pure`, `effect(...)`, `restrict(...)`, `pre(...)`, `post(...)` and `unhandled(trap)` on an `effect` declaration"
W AX3039 346-axtag-key-typo.ax:31:6-11 axtag-key-typo "AXTAG key typo on `other`: unrecognised key `effct`; did you mean `effect`?" ?"an unrecognised key is recorded and never checked, so the claim this looks like was not made; the keys that are checked are `pure`, `effect(...)`, `restrict(...)`, `pre(...)`, `post(...)` and `unhandled(trap)` on an `effect` declaration"
