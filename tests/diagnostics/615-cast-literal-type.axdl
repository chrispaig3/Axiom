E AX3002 615-cast-literal-type.ax:40:13-14 undefined-type "a literal is not a type, and `cast` takes a type first" #"not a type" !"an unconvertible shape here used to answer a silent wildcard, which unifies with everything: the cast was performed, the result had no type anyone could object to, and a function returning it satisfied any declaration" ?"write the type to cast TO - `(cast Int e)` - or, for an ascription, `(:: e Int)`"
