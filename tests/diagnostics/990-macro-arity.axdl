E AX3018 990-macro-arity.ax:8:13-16 macro-arity "macro `two` takes 2 arguments, but was given 1" !"a macro is a rewrite, not a function: a missing argument would leave the parameter's own name in the generated code, and a surplus one would be dropped without being evaluated" ?"pass exactly as many arguments as the macro declares parameters"
