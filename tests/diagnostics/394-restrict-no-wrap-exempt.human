[1;31merror[AX3049][0m[1m: `+` in the body of `handRolled`, which claims `restrict(no-wrap)`[0m
  [1;34m--> [0m394-restrict-no-wrap-exempt.ax:80:17
[1;34m   |[0m
[1;34m80 |[0m         (set i (+ i 1)))
[1;34m   |[0m                 [1;31m^[0m
[1;34m   |[0m
  [1;34m = [0m[1;32mhelp: [0m`no-wrap` is LOCAL, like `no-cast`: on `Int` operands `+`, `-` and `*` lower to plain `add`/`sub`/`mul` with no overflow check, so a silent wraparound is an act this body performs by writing the raw operator, not a fact the effect row or call graph carries - it is refused where it is written, and a callee's arithmetic is the callee's claim to make. `stdlib/Err.ax` has the checked alternative for each: `addChecked`, `subChecked`, `mulChecked`, every one `(-> Int Int (Result Int Error))`. Calling one pulls `Alloc` into this body's effect row (constructing the `Result` allocates), so `no-wrap` cannot be satisfied together with `no-alloc` or `pure` by a body that needs arithmetic - write the checked call and match its `Err`, accept the row, or delete the tag. TWO OPERATORS ARE NOT REFUSED, because neither can wrap: the same three spellings on `Float` operands, which lower to `fadd`/`fsub`/`fmul`, and the `for` keyword's own counter increment, which the parser writes and whose loop guard bounds it
  [1;34m = [0m[1;32mhelp: [0mrun `axiom explain AX3049` for a full explanation

[1;31merror[AX3049][0m[1m: `*` in the body of `loopBody`, which claims `restrict(no-wrap)`[0m
  [1;34m--> [0m394-restrict-no-wrap-exempt.ax:93:19
[1;34m   |[0m
[1;34m93 |[0m         (set acc (* acc i)))
[1;34m   |[0m                   [1;31m^[0m
[1;34m   |[0m
  [1;34m = [0m[1;32mhelp: [0m`no-wrap` is LOCAL, like `no-cast`: on `Int` operands `+`, `-` and `*` lower to plain `add`/`sub`/`mul` with no overflow check, so a silent wraparound is an act this body performs by writing the raw operator, not a fact the effect row or call graph carries - it is refused where it is written, and a callee's arithmetic is the callee's claim to make. `stdlib/Err.ax` has the checked alternative for each: `addChecked`, `subChecked`, `mulChecked`, every one `(-> Int Int (Result Int Error))`. Calling one pulls `Alloc` into this body's effect row (constructing the `Result` allocates), so `no-wrap` cannot be satisfied together with `no-alloc` or `pure` by a body that needs arithmetic - write the checked call and match its `Err`, accept the row, or delete the tag. TWO OPERATORS ARE NOT REFUSED, because neither can wrap: the same three spellings on `Float` operands, which lower to `fadd`/`fsub`/`fmul`, and the `for` keyword's own counter increment, which the parser writes and whose loop guard bounds it
  [1;34m = [0m[1;32mhelp: [0mrun `axiom explain AX3049` for a full explanation

[1;31merror[AX3049][0m[1m: `+` in the body of `mixed`, which claims `restrict(no-wrap)`[0m
   [1;34m--> [0m394-restrict-no-wrap-exempt.ax:102:18
[1;34m    |[0m
[1;34m102 |[0m (fn (mixed f n) (+ (__floatToInt (+ f 1.5)) n))
[1;34m    |[0m                  [1;31m^[0m
[1;34m    |[0m
   [1;34m = [0m[1;32mhelp: [0m`no-wrap` is LOCAL, like `no-cast`: on `Int` operands `+`, `-` and `*` lower to plain `add`/`sub`/`mul` with no overflow check, so a silent wraparound is an act this body performs by writing the raw operator, not a fact the effect row or call graph carries - it is refused where it is written, and a callee's arithmetic is the callee's claim to make. `stdlib/Err.ax` has the checked alternative for each: `addChecked`, `subChecked`, `mulChecked`, every one `(-> Int Int (Result Int Error))`. Calling one pulls `Alloc` into this body's effect row (constructing the `Result` allocates), so `no-wrap` cannot be satisfied together with `no-alloc` or `pure` by a body that needs arithmetic - write the checked call and match its `Err`, accept the row, or delete the tag. TWO OPERATORS ARE NOT REFUSED, because neither can wrap: the same three spellings on `Float` operands, which lower to `fadd`/`fsub`/`fmul`, and the `for` keyword's own counter increment, which the parser writes and whose loop guard bounds it
   [1;34m = [0m[1;32mhelp: [0mrun `axiom explain AX3049` for a full explanation

compilation failed due to 3 previous errors
