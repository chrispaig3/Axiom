E AX3008 390-nullary-lambda.ax:5:6-12 semantic-error "a lambda with no parameters can never be called" #"this lambda takes no parameters" !"`(f)` and `f` are the same expression, so no syntax applies a nullary closure; it used to evaluate to the closure record's address" ?"give it a parameter, or write a top-level `(fn (name) body)` - a bare reference to a nullary function does call it"
