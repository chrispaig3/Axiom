; `if` without its else: an `if` always ends in its else operand
; (reference.md), so a chain that stops where a pair is complete is
; a chain without its else - write it out.
(:: main Int)
(fn (main) (if (== 1 1) 41))
