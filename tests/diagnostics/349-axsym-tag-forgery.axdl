W AX3039 349-axsym-tag-forgery.ax:60:6-14 axtag-key-typo "unrecognised AXTAG key `effects` on `collided`; did you mean `effect`?" ?"an unrecognised key is recorded and never checked, so the claim this looks like was not made; the keys that are checked are `pure` and `effect(...)`"
