[1;31merror[AX3049][0m[1m: `/` in the body of `divides`, which claims `restrict(no-untrapped)`[0m
  [1;34m--> [0m396-restrict-no-untrapped.ax:46:4
[1;34m   |[0m
[1;34m46 |[0m   (/ a b))
[1;34m   |[0m    [1;31m^[0m
[1;34m   |[0m
  [1;34m = [0m[1;32mhelp: [0m`no-untrapped` is LOCAL, like `no-cast` and `no-wrap`: on `Int` operands `/` and `%` trap on a zero divisor (exit 72) but are undefined on the single `INT_MIN / -1` corner, and `<<` and `>>` are undefined on an out-of-range shift amount with no runtime check at all - so the untrapped use is an act this body performs by writing the raw operator, not a fact the effect row or call graph carries. `stdlib/Err.ax` has the checked alternative for each: `divChecked`, `remChecked`, `shlChecked`, `shrChecked`, every one `(-> Int Int (Result Int Error))`. Calling one pulls `Alloc` into this body's effect row (constructing the `Result` allocates), so `no-untrapped` cannot be satisfied together with `no-alloc` or `pure` by a body that needs these operations - write the checked call and match its `Err`, accept the row, or delete the tag. ONE SPELLING IS NOT REFUSED: `/` on `Float` operands, which lowers to `fdiv` with no integer trap to require, and whose checked fix is `(-> Int Int (Result Int Error))` and does not typecheck against a `Float`
  [1;34m = [0m[1;32mhelp: [0mrun `axiom explain AX3049` for a full explanation

[1;31merror[AX3049][0m[1m: `%` in the body of `nested`, which claims `restrict(no-untrapped)`[0m
  [1;34m--> [0m396-restrict-no-untrapped.ax:52:15
[1;34m   |[0m
[1;34m52 |[0m     (let ((d (% a b)))
[1;34m   |[0m               [1;31m^[0m
[1;34m   |[0m
  [1;34m = [0m[1;32mhelp: [0m`no-untrapped` is LOCAL, like `no-cast` and `no-wrap`: on `Int` operands `/` and `%` trap on a zero divisor (exit 72) but are undefined on the single `INT_MIN / -1` corner, and `<<` and `>>` are undefined on an out-of-range shift amount with no runtime check at all - so the untrapped use is an act this body performs by writing the raw operator, not a fact the effect row or call graph carries. `stdlib/Err.ax` has the checked alternative for each: `divChecked`, `remChecked`, `shlChecked`, `shrChecked`, every one `(-> Int Int (Result Int Error))`. Calling one pulls `Alloc` into this body's effect row (constructing the `Result` allocates), so `no-untrapped` cannot be satisfied together with `no-alloc` or `pure` by a body that needs these operations - write the checked call and match its `Err`, accept the row, or delete the tag. ONE SPELLING IS NOT REFUSED: `/` on `Float` operands, which lowers to `fdiv` with no integer trap to require, and whose checked fix is `(-> Int Int (Result Int Error))` and does not typecheck against a `Float`
  [1;34m = [0m[1;32mhelp: [0mrun `axiom explain AX3049` for a full explanation

[1;31merror[AX3049][0m[1m: `<<` in the body of `nested`, which claims `restrict(no-untrapped)`[0m
  [1;34m--> [0m396-restrict-no-untrapped.ax:53:8
[1;34m   |[0m
[1;34m53 |[0m       (<< d c))
[1;34m   |[0m        [1;31m^^[0m
[1;34m   |[0m
  [1;34m = [0m[1;32mhelp: [0m`no-untrapped` is LOCAL, like `no-cast` and `no-wrap`: on `Int` operands `/` and `%` trap on a zero divisor (exit 72) but are undefined on the single `INT_MIN / -1` corner, and `<<` and `>>` are undefined on an out-of-range shift amount with no runtime check at all - so the untrapped use is an act this body performs by writing the raw operator, not a fact the effect row or call graph carries. `stdlib/Err.ax` has the checked alternative for each: `divChecked`, `remChecked`, `shlChecked`, `shrChecked`, every one `(-> Int Int (Result Int Error))`. Calling one pulls `Alloc` into this body's effect row (constructing the `Result` allocates), so `no-untrapped` cannot be satisfied together with `no-alloc` or `pure` by a body that needs these operations - write the checked call and match its `Err`, accept the row, or delete the tag. ONE SPELLING IS NOT REFUSED: `/` on `Float` operands, which lowers to `fdiv` with no integer trap to require, and whose checked fix is `(-> Int Int (Result Int Error))` and does not typecheck against a `Float`
  [1;34m = [0m[1;32mhelp: [0mrun `axiom explain AX3049` for a full explanation

[1;31merror[AX3049][0m[1m: `>>` in the body of `shifts`, which claims `restrict(no-untrapped)`[0m
  [1;34m--> [0m396-restrict-no-untrapped.ax:59:4
[1;34m   |[0m
[1;34m59 |[0m   (>> a n))
[1;34m   |[0m    [1;31m^^[0m
[1;34m   |[0m
  [1;34m = [0m[1;32mhelp: [0m`no-untrapped` is LOCAL, like `no-cast` and `no-wrap`: on `Int` operands `/` and `%` trap on a zero divisor (exit 72) but are undefined on the single `INT_MIN / -1` corner, and `<<` and `>>` are undefined on an out-of-range shift amount with no runtime check at all - so the untrapped use is an act this body performs by writing the raw operator, not a fact the effect row or call graph carries. `stdlib/Err.ax` has the checked alternative for each: `divChecked`, `remChecked`, `shlChecked`, `shrChecked`, every one `(-> Int Int (Result Int Error))`. Calling one pulls `Alloc` into this body's effect row (constructing the `Result` allocates), so `no-untrapped` cannot be satisfied together with `no-alloc` or `pure` by a body that needs these operations - write the checked call and match its `Err`, accept the row, or delete the tag. ONE SPELLING IS NOT REFUSED: `/` on `Float` operands, which lowers to `fdiv` with no integer trap to require, and whose checked fix is `(-> Int Int (Result Int Error))` and does not typecheck against a `Float`
  [1;34m = [0m[1;32mhelp: [0mrun `axiom explain AX3049` for a full explanation

compilation failed due to 4 previous errors
