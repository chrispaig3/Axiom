W AX3053 345-effect-unverifiable.ax:55:6-10 unhandled-operation "`main` reaches operation `log` of effect `Console` with no handler: the process would write `axiom: unhandled effect` and exit 71 there - main -> viaDirect -> usesLog" #"reaches `Console` with nothing handling it" ^30:6-13:"`usesLog` performs it" ?"install one - `(handle BODY (E) HANDLER)` around the call - or, if an unhandled operation of this effect is a deliberate abort as `Assert`'s is, write `;@axiom:unhandled(trap)` above its `effect` declaration"
W AX3038 345-effect-unverifiable.ax:50:12-13 effect-unverifiable "handled effects cannot be checked: the body calls a value the compiler could not resolve" ?"name the function at the call, or widen the handled list: an unresolved call may perform any effect, and reaching one with no handler aborts the process"
E AX3011 345-effect-unverifiable.ax:44:12-19 effect-mismatch "effect mismatch: unhandled effect `Console`"
