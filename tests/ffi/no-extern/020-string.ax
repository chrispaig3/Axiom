; Strings without the FFI. The `Str` paths are the ones the FFI's
; marshalling layer reads, so a regression here would look like an FFI
; bug when it is not one.
(import IO)

(import Str)

;@axiom:effect(io)
(pub fn (main)
  {
    (println (strConcat "hello" " world"))
    0
  }
)
; expect 0
