; expect 42
; A `type` alias in argument position, which used to be AX3004 while
; the same alias in return position was transparent.
(type Count = Int)

(:: h (-> Count Int))
(fn (h c) c)

(:: main Int)
(fn (main) (h 42))
