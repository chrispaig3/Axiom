; expect 42
(import Sys)

(:: main Int)

;@axiom:effect(io)
(fn (main)
  (let ((fd (sysOpenPath (__addr "in.ax") oRdonly)))
    (if (> fd 2)
      42
      (if (< fd 0)
        7
        8
      )
    )
  )
)
