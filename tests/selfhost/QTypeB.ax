; A second `Point`, deliberately colliding with `QTypeA`'s: a bare
; reference is `AX3044`, and only qualification picks one. Its single
; field keeps the layout distinct, so a wrong pick answers wrong.
(pub struct Point (v : Int))
