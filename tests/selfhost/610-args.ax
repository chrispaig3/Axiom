; expect 1
; The conformance runner invokes binaries with no arguments, so argc
; is exactly 1: the program name, captured by the @main wrapper both
; compilers now emit before the user's (renamed) main runs.
(:: main Int)

(fn (main) __argc)
