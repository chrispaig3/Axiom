; expect 42
(import Str)

(import Sys)

; in.ax is this file; its first line is the `; expect` marker, so a
; successful read is any non-zero length.
(:: main Int)

;@axiom:effect(io)
(fn (main)
  (let ((src (sysReadFile (__addr "in.ax"))))
    (if (> (strLen src) 0)
      42
      7
    )
  )
)
