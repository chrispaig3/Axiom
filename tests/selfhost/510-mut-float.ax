; expect 48
; A mutable Float: the binding keeps its float-ness across stores, so
; the loop doubles with fmul and the compare is an fcmp. 1.5 doubled
; five times passes 40.
(:: main Int)
(fn (main) (let ((mut x 1.5)) { (while (< x 40.0) (set x (* x 2.0))) (__floatToInt x) }))
