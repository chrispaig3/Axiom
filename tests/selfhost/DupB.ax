; The other half. The two answers are far apart on purpose: a fix that
; merely silenced `AX3014` without resolving to the named module would
; still pass a test that only counted diagnostics.
(pub :: dup (-> Int Int))
(pub fn (dup n) (+ n 100))
