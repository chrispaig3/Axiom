Source stamp:          3192f3653a336d0d541d543d6c3d6315af6558c9c5c1d81365cc1562ba07c5a4
Source files:          60
Generated on:          2026-09-02T01:12:45Z
Regenerate with:       scripts/reseed.sh
Verified by:           scripts/bootstrap-from-seed.sh, scripts/check-seed-provenance.sh, scripts/check-seed-lineage.sh

The source stamp is `gate_seed_source_stamp` (scripts/lib/gate.sh) over
the `self_host/**.ax` and `stdlib/**.ax` bytes these seeds were
generated from: the path list, then every byte, hashed. It is the
CHECKABLE claim here, and `scripts/check-seed-provenance.sh` checks it -
it finds the commit that last touched the six `.ll` files - not this
directory, which also holds metadata about them - requires that
commit's sources to hash to this line, and then regenerates all six
seeds from them and requires the result to be byte-identical.
