[package]
name = "axiom-ffi-macros"
description = "The #[axiom_export] attribute: generates the C-ABI shim Axiom links against."
version.workspace = true
edition.workspace = true
license.workspace = true

[lib]
proc-macro = true

[dependencies]
syn = { workspace = true }
quote = { workspace = true }
proc-macro2 = { workspace = true }
